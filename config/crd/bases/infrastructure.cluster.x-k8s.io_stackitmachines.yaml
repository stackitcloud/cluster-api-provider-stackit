---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: stackitmachines.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: StackitMachine
    listKind: StackitMachineList
    plural: stackitmachines
    shortNames:
    - stim
    singular: stackitmachine
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Cluster to which this resource belongs
      jsonPath: .metadata.labels['cluster\.x-k8s\.io/cluster-name']
      name: Cluster
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - description: STACKIT server state
      jsonPath: .status.instanceState
      name: State
      type: string
    - jsonPath: .spec.machineType
      name: Machine Type
      type: string
    - jsonPath: .status.addresses[?(@.type=='InternalIP')].address
      name: IP
      type: string
    - jsonPath: .status.availabilityZone
      name: Zone
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StackitMachine is the Schema for the stackitmachines API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of StackitMachine
            properties:
              additionalLabels:
                additionalProperties:
                  type: string
                description: additionalLabels is merged into the labels applied to
                  STACKIT resources.
                maxProperties: 64
                type: object
              availabilityZone:
                description: availabilityZone selects a STACKIT availability zone
                  (e.g. eu01-1).
                pattern: ^[a-z]{2}[0-9]{2}-[a-z0-9]+$
                type: string
              imageID:
                description: imageID is the ID of the STACKIT image to boot from.
                minLength: 1
                pattern: ^[0-9a-fA-F-]{36}$
                type: string
              machineType:
                description: machineType selects the STACKIT machine flavor (e.g.
                  c2i.2).
                minLength: 1
                pattern: ^[a-z0-9][a-z0-9.-]*[a-z0-9]$
                type: string
              network:
                description: network references the STACKIT network the VM should
                  be attached to.
                properties:
                  id:
                    description: id is the STACKIT network ID.
                    minLength: 1
                    pattern: ^[0-9a-fA-F-]{36}$
                    type: string
                required:
                - id
                type: object
              providerID:
                description: |-
                  providerID is the unique identifier the cloud-provider will use to
                  reconcile this machine with the corresponding Node. It is set by the
                  controller once the VM has been created.
                type: string
              rootVolume:
                description: rootVolume configures the root disk for the VM.
                properties:
                  deleteOnTermination:
                    description: |-
                      deleteOnTermination causes the root volume to be deleted when the VM is
                      deleted.
                    type: boolean
                  performanceClass:
                    description: performanceClass selects a STACKIT storage performance
                      class.
                    minLength: 1
                    type: string
                  sizeGiB:
                    description: sizeGiB is the size of the root volume in GiB.
                    minimum: 0
                    type: integer
                type: object
              securityGroups:
                description: securityGroups is a list of STACKIT security group IDs
                  applied to the VM.
                items:
                  pattern: ^[0-9a-fA-F-]{36}$
                  type: string
                type: array
              sshKeyName:
                description: sshKeyName is the name of an existing SSH key in the
                  STACKIT project.
                maxLength: 127
                pattern: ^[A-Za-z0-9_.@-]+$
                type: string
            required:
            - imageID
            - machineType
            - network
            type: object
          status:
            description: status defines the observed state of StackitMachine
            properties:
              addresses:
                description: addresses contains the IP addresses associated with the
                  VM.
                items:
                  description: MachineAddress contains information for the node's
                    address.
                  properties:
                    address:
                      description: address is the machine address.
                      maxLength: 256
                      minLength: 1
                      type: string
                    type:
                      description: type is the machine address type, one of Hostname,
                        ExternalIP, InternalIP, ExternalDNS or InternalDNS.
                      enum:
                      - Hostname
                      - ExternalIP
                      - InternalIP
                      - ExternalDNS
                      - InternalDNS
                      type: string
                  required:
                  - address
                  - type
                  type: object
                type: array
              availabilityZone:
                description: availabilityZone is the STACKIT availability zone where
                  the VM was placed.
                type: string
              conditions:
                description: conditions represent the current state of the StackitMachine
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              initialization:
                description: initialization provides Cluster API v1beta2 contract
                  state.
                properties:
                  provisioned:
                    description: provisioned is true when the VM is provisioned.
                    type: boolean
                type: object
              instanceID:
                description: instanceID is the STACKIT server ID assigned to this
                  machine.
                type: string
              instanceState:
                description: instanceState is the latest known state of the STACKIT
                  server.
                type: string
              providerID:
                description: providerID is the providerID written back to the owning
                  Machine.
                type: string
              ready:
                description: |-
                  ready indicates the underlying VM is provisioned and known to the
                  provider.
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
