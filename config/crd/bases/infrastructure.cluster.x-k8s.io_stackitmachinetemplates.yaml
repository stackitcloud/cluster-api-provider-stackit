---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: stackitmachinetemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: StackitMachineTemplate
    listKind: StackitMachineTemplateList
    plural: stackitmachinetemplates
    shortNames:
    - stimt
    singular: stackitmachinetemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.template.spec.machineType
      name: Machine Type
      type: string
    - jsonPath: .spec.template.spec.imageID
      name: Image ID
      type: string
    - jsonPath: .spec.template.spec.rootVolume.sizeGiB
      name: Disk GiB
      type: integer
    - jsonPath: .spec.template.spec.availabilityZone
      name: Zone
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .spec.template.spec.network.id
      name: Network ID
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StackitMachineTemplate is the Schema for the stackitmachinetemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of StackitMachineTemplate
            properties:
              template:
                description: template wraps the StackitMachine spec used to create
                  new machines.
                properties:
                  metadata:
                    description: |-
                      metadata is the standard object's metadata.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
                    minProperties: 1
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          annotations is an unstructured key value map stored with a resource that may be
                          set by external tools to store and retrieve arbitrary metadata. They are not
                          queryable and should be preserved when modifying objects.
                          More info: http://kubernetes.io/docs/user-guide/annotations
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          labels is a map of string keys and values that can be used to organize and categorize
                          (scope and select) objects. May match selectors of replication controllers
                          and services.
                          More info: http://kubernetes.io/docs/user-guide/labels
                        type: object
                    type: object
                  spec:
                    description: |-
                      spec is the StackitMachineSpec that will be used to create the
                      StackitMachine.
                    properties:
                      additionalLabels:
                        additionalProperties:
                          type: string
                        description: additionalLabels is merged into the labels applied
                          to STACKIT resources.
                        maxProperties: 64
                        type: object
                      availabilityZone:
                        description: availabilityZone selects a STACKIT availability
                          zone (e.g. eu01-1).
                        pattern: ^[a-z]{2}[0-9]{2}-[a-z0-9]+$
                        type: string
                      imageID:
                        description: imageID is the ID of the STACKIT image to boot
                          from.
                        minLength: 1
                        pattern: ^[0-9a-fA-F-]{36}$
                        type: string
                      machineType:
                        description: machineType selects the STACKIT machine flavor
                          (e.g. c2i.2).
                        minLength: 1
                        pattern: ^[a-z0-9][a-z0-9.-]*[a-z0-9]$
                        type: string
                      network:
                        description: network references the STACKIT network the VM
                          should be attached to.
                        properties:
                          id:
                            description: id is the STACKIT network ID.
                            minLength: 1
                            pattern: ^[0-9a-fA-F-]{36}$
                            type: string
                        required:
                        - id
                        type: object
                      providerID:
                        description: |-
                          providerID is the unique identifier the cloud-provider will use to
                          reconcile this machine with the corresponding Node. It is set by the
                          controller once the VM has been created.
                        type: string
                      rootVolume:
                        description: rootVolume configures the root disk for the VM.
                        properties:
                          deleteOnTermination:
                            description: |-
                              deleteOnTermination causes the root volume to be deleted when the VM is
                              deleted.
                            type: boolean
                          performanceClass:
                            description: performanceClass selects a STACKIT storage
                              performance class.
                            minLength: 1
                            type: string
                          sizeGiB:
                            description: sizeGiB is the size of the root volume in
                              GiB.
                            minimum: 0
                            type: integer
                        type: object
                      securityGroups:
                        description: securityGroups is a list of STACKIT security
                          group IDs applied to the VM.
                        items:
                          pattern: ^[0-9a-fA-F-]{36}$
                          type: string
                        type: array
                      sshKeyName:
                        description: sshKeyName is the name of an existing SSH key
                          in the STACKIT project.
                        maxLength: 127
                        pattern: ^[A-Za-z0-9_.@-]+$
                        type: string
                    required:
                    - imageID
                    - machineType
                    - network
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
